<%= form_tag(user_favorites_path(current_user), method: :post, remote: true, :class => 'add-to-favorite') do %>
  <%= hidden_field_tag 'tobe_added_user_id', @user.id %>
  <%= submit_tag " ♥ Add to favorite", id: 'add_favorite' %>
<%end %>