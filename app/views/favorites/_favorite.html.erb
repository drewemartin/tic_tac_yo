<%= form_tag(user_favorites_path(current_user), method: :post, :class => 'add-to-favorite') do |f| %>
  <%= hidden_field_tag 'tobe_added_user_id', @user.id %>
  <%= submit_tag "Add to favorite"  %>
<%end %>