<%= form_tag(user_blocks_path(current_user), method: :post, remote: true, :class => 'add-to-block') do |f| %>
  <%= hidden_field_tag 'tobe_added_user_id', @user.id %>
  <%= submit_tag "X Add to block", class: 'block' %>
<%end %>